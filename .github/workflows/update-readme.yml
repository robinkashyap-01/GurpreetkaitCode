name: Update README
on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity
    steps:
      - uses: actions/checkout@v2
      - uses:  GurpreetkaitCode/README@main
        env:
          GITHUB_TOKEN: ${{ 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 }}

